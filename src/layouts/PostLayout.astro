---
import BaseLayoutV2 from "./BaseLayout-v2.astro";
import Comment from "@components/Comment.astro";
import RelativeArticle from "@components/RelativeArticle.astro";
import SubscribeEmail from "@components/SubscribeEmail.astro";
import { DatetimeUtils } from "@utils/Datetime";

const { frontmatter } = Astro.props;

const formatPubTime = DatetimeUtils.formatDateToDDMMYYYY(new Date(frontmatter.pubDate));
---

<BaseLayoutV2 pageTitle={frontmatter.title}>
  <!-- <p>
    <strong>Written by</strong>: {frontmatter.author} on <em>
      {frontmatter.pubDate}
    </em>
  </p>
  <div class="tags">
    {
      frontmatter.tags.map((tag: string) => (
        <p class="tag">
          <a href={MyPath(`tags/${tag}`)}>{tag}</a>
        </p>
      ))
    }
  </div> -->

  <!-- 
Install the "flowbite-typography" NPM package to apply styles and format the article content: 

URL: https://flowbite.com/docs/components/typography/ 
-->

  <!-- <main class="pt-8 pb-16 lg:pt-16 lg:pb-24 bg-white dark:bg-gray-900 antialiased">
  <div class="flex justify-between px-4 mx-auto max-w-screen-xl "> -->
  <article
    class="min-h-96 w-full format format-sm sm:format-base lg:format-lg format-blue dark:format-invert"
  >
    <header class="mb-4 lg:mb-6 not-format">
      <address class="flex items-center mb-6 not-italic">
        <div
          class="inline-flex items-center mr-3 text-sm text-gray-900 dark:text-white"
        >
          <img
            class="mr-4 w-16 h-16 rounded-full"
            src="https://flowbite.com/docs/images/people/profile-picture-2.jpg"
            alt="Jese Leos"
          />
          <div>
            <a
              href="#"
              rel="author"
              class="text-xl font-bold text-gray-900 dark:text-white"
            >
              {frontmatter.author}
            </a>
            <p class="text-base text-gray-500 dark:text-gray-400">
              Graphic Designer, educator & CEO Flowbite
            </p>
            <p class="text-base text-gray-500 dark:text-gray-400">
              <div title={formatPubTime}>{formatPubTime}</div>
            </p>
          </div>
        </div>
      </address>
      <h1
        class="mb-4 text-3xl font-extrabold leading-tight text-gray-900 lg:mb-6 lg:text-4xl dark:text-white"
      >
        {frontmatter.title}
      </h1>
    </header>
    <slot />
    <!-- <Comment /> -->
  </article>
  <!-- </div>
</main> -->

  <!-- <RelativeArticle /> -->

  <SubscribeEmail />
</BaseLayoutV2>
<style>
  a {
    color: #00539f;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
  }

  .tag {
    margin: 0.25em;
    border: dotted 1px #a1a1a1;
    border-radius: 0.5em;
    padding: 0.5em 1em;
    font-size: 1.15em;
    background-color: #f8fcfd;
  }
</style>
